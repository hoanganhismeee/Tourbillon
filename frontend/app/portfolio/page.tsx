// Portfolio index — intentionally blank.
// The maker's story now lives at /portfolio/tourbillon. This route is a
// placeholder reserved for a future portfolio landing/index.
export default function PortfolioPage() {
  return <main className="min-h-screen" />;
}
